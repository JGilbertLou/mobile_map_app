import 'config.dart';

class Trip {
  final String id;
  final String origin;
  final String destination;
  final String velocity;
  final String time;

  Trip(this.id, this.origin, this.destination, this.velocity, this.time);
}