import 'config.dart';

class Trip {
  final String origin;
  final String destination;
  final String velocity;
  final String time;

  Trip(this.origin, this.destination, this.velocity, this.time);
}